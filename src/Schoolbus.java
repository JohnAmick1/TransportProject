public class Schoolbus extends TransportVehicle {
    Schoolbus(){
        setPassengerCount(30);
        setSafetyCoefficient(0.5);
        setAverageSpeed(45);
        // ***figure out more attributes to a schoolbus to consider in random simulation
    }
}
